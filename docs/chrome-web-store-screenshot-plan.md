# Chrome Web Store Screenshot Plan

Use the actual unpacked extension version shown in `manifest.json`. Do not add mock functionality.

Capture clean screenshots with representative, non-private sample data:

1. **Calculator and completed result** — popup with Dallas, TX → Atlanta, GA, score, all-in RPM, estimated profit, total miles, and Reload Score visible.
2. **Alert match and broker guidance** — show a genuine Match result plus minimum rate and broker message.
3. **Saved loads** — show two or three locally saved sample loads with different scores/statuses; avoid real broker or customer details.
4. **Highlighted-text workflow** — show selected public/sample text and the popup's parsed-field confirmation; do not use private authenticated data.
5. **Share result** — show the Copy LoadScore Result control and success confirmation.
6. **Timing and notifications** — after Build 2, show optional timing fields and the explicit notification-off/on settings.

Recommended capture process:

- Reload the unpacked extension after pulling the release commit.
- Use a clean Chrome profile or clear demo-only local extension data.
- Use consistent sample data and no personal information.
- Capture at the current Chrome Web Store-required dimensions in the dashboard; requirements can change, so verify before export.
- Keep the popup fully visible and avoid browser developer tools in final images.

FOUNDER ACTION REQUIRED: capture and upload the final screenshots after reloading the final packaged version.
